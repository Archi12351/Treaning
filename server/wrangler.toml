name = "deutsch-ai-proxy"
main = "worker.js"
compatibility_date = "2026-01-01"

# Set the real secret with:
#   wrangler secret put ANTHROPIC_API_KEY
# Never put the actual key value in this file — it's committed to git.
